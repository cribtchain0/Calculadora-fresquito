{
  "name": "fresquito-finanzas",
  "version": "1.0.0",
  "private": true,
  "description": "Libro de finanzas, inventario y costeo de Fresquito Gourmet Pops",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently -n servidor,cliente -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
